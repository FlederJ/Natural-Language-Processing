from utils import load_save, bigrams, find_words
# TODO: bei "don t" nicht nur "t" als nächstes Wort nehmen => weitergeführter Text
# evtl "english words with apostrophes" aus nltk finden und damit vergleichen

#load_save.load_or_create_phraser(bigrams.gberg_sent_bigram(
#    bigrams.gberg_sents))

import pytest
pytest.main()